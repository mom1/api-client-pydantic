  - {{subject}}{{#if issues}} (Issues:{{#each issues}} [`{{text}}`]({{link}}){{/each}}){{/if}}
{{#if body}}{{#unless is_dep}}
  {{# each (split_by_line body) as |body_line|}}
    {{#if body_line}}{{body_line}}{{/if}}
  {{/each}}
{{/unless}}{{/if}}
